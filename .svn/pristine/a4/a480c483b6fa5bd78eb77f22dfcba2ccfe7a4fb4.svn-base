import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PublicAccountManagementComponent } from './public-account-management.component';

describe('PublicAccountManagementComponent', () => {
  let component: PublicAccountManagementComponent;
  let fixture: ComponentFixture<PublicAccountManagementComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PublicAccountManagementComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PublicAccountManagementComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
