import { FixHeightDirective } from './fix-height.directive';

describe('FixHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new FixHeightDirective();
    expect(directive).toBeTruthy();
  });
});
